{
  "name": "neocustomwithdist",
  "version": "1.0.0",
  "description": "GitHub Action to generate SBOM using cdxgen",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=dist/index.js --platform=node",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@cyclonedx/cdxgen": "^11.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
