{
  "name": "neocustomwithdist",
  "version": "1.0.0",
  "description": "GitHub Action to generate SBOM using cdxgen",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --outfile=dist/index.js --loader:.json=json && mkdir -p dist/data && cp node_modules/@cyclonedx/cdxgen/data/lic-mapping.json dist/data/",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@cyclonedx/cdxgen": "^11.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
